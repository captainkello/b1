<%= form_for(@wig) do |f| %>
  <% if @wig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wig.errors.count, "error") %> prohibited this wig from being saved:</h2>

      <ul>
      <% @wig.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :style %><br />
    <%= f.text_field :style %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
